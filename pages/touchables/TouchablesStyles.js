import { StyleSheet } from 'react-native';

export const touchables_styles = StyleSheet.create(
  {
    safe_area_view: {
      flex: 1,
      backgroundColor: 'dodgerblue',
      alignItems: 'center',
      justifyContent: 'center'
    },
    image: {
      width: 100,
      height: 150
    }
  }
)