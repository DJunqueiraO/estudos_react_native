import { StyleSheet } from 'react-native';

export const buttons_styles = StyleSheet.create(
  {
    safe_area_view: {
      flex: 1,
      backgroundColor: 'red',
      color: 'white',
      fontWeight: 'bold',
      // alignItems: 'center',
      // justifyContent: 'center'
    }
  }
)